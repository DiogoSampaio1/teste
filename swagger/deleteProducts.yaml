tags:
  - 'Products'
summary: 'Delete Products'
parameters:
  - in: query
    name: product_id
    required: true
    schema:
      type: integer
  - in: query
    name: product_code
    required: true
    schema:
      type: string
responses:
  '200':
    description: 'Product deleted successfully'
  '400':
    description: 'Invalid input'
  '404':
    description: 'Product does not exist or is deleted'
